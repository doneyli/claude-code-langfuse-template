[project]
name = "claude-code-langfuse-hook"
version = "1.0.0"
description = "Claude Code session observability hook for Langfuse"
requires-python = ">=3.11"
license = "MIT"
dependencies = ["langfuse>=3.0,<4.0"]

[project.scripts]
langfuse-hook = "hooks.langfuse_hook:main"
